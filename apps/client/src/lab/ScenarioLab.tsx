/** Scenario Lab (dev only; excluded from production builds). Filled in by M3 step 3.x. */
export function ScenarioLab() {
  return (
    <main class="setup">
      <h2>Scenario Lab</h2>
      <p>Load worked examples E1–E9 or a custom position and step through the reaction chain.</p>
    </main>
  );
}
